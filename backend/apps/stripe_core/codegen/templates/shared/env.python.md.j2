# Stripe — copy to backend/.env for local dev (never commit real keys)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
APP_URL={{ app_url }}
# Local dev only — use docker-compose Postgres or sqlite. On Railway, use a
# Variables reference to your Postgres service (e.g. {% raw %}${{Postgres.DATABASE_URL}}{% endraw %}).
DATABASE_URL=postgresql://agripay:agripay@localhost:5432/agripay
