# Stripe - {{ profile.display_name }} setup

Generated by Stripe Installer for **{{ framework }}**.

## Webhook URL
`{{ app_url }}{{ webhook_path }}`

## Environment
Copy `.env.example` and set:
- `STRIPE_SECRET_KEY`
- `STRIPE_PUBLISHABLE_KEY`
- `STRIPE_WEBHOOK_SECRET`
- `APP_URL`

## Next steps
1. Wire generated routes into your app.
2. Run `psql $DATABASE_URL -f db/schema.sql` if using Django DB sync.
3. Test checkout and billing portal.

{% if tiers %}
## Provisioned tiers
{% for t in tiers %}
- **{{ t.tier }}**: `{{ t.price_id }}` ({{ t.label }})
{% endfor %}
{% else %}
Run provisioning first to create Stripe products and prices.
{% endif %}
