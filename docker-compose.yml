services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  # Optional — use SQLite by default; uncomment for Postgres in production
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: stripe_installer
  #     POSTGRES_PASSWORD: stripe_installer
  #     POSTGRES_DB: stripe_installer
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pg_data:/var/lib/postgresql/data

volumes:
  redis_data:
  # pg_data:
